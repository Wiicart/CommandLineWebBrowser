package net.wiicart.webcli.screen;

import com.googlecode.lanterna.gui2.Window;
import com.googlecode.lanterna.gui2.WindowBasedTextGUI;
import org.jetbrains.annotations.NotNull;

import java.util.Set;

public class UnreachableSiteScreen {

    /*private static final Set<Window.Hint> ENTRY_HINTS = Set.of(Window.Hint.CENTERED, Window.Hint.FIT_TERMINAL_WINDOW);

    private final @NotNull WindowBasedTextGUI gui;
    private final @NotNull String address;

    private boolean execute = false;

    public UnreachableSiteScreen(@NotNull WindowBasedTextGUI gui, @NotNull String address) {
        this.gui = gui;
        this.address = address;
    }

    public UnreachableSiteScreen show() {
        Window window = createWindow();
        gui.addWindowAndWait(window); //todo unnull
        execute = true;
        executeRunnables();
        return this;
    }

    private Window createWindow() {
        return null;
    }


    @Override
    boolean readyToExecute() {
        return true;
    }*/
}
